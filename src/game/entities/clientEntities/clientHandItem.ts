import { ThreeScene } from "../../scenes/threeScene";
import { ClientEntity } from "./clientEntity";

export class ClientHandItem extends ClientEntity
{
    public update(delta: number)
    {
        super.update(delta);
    }
}